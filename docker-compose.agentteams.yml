# 正式 AgentTeams 同机部署覆盖层：把 RevGuard API 接入 Worker 所在网络。
services:
  revguard-api:
    networks:
      default: {}
      agentteams:
        aliases:
          - revguard-api
          - revguard-api.internal

networks:
  agentteams:
    external: true
    name: agentteams-net
