# 两份差距文档的完成审计（2026-08-31）

范围：`RevGuard_人工审批HITL对齐分析.md` 与 `design-gap-mcp-gateway.md`，
以及现有录制 WebUI、旁白和部署环境。以代码、测试、实际持久化任务与本轮截图为准，
不将文档的设计建议或默认配置当成已验收事实。

## 用户确认的本轮范围

2026-08-31，用户明确确认：本轮使用 AgentTeams `admin` 完成复赛演示，企业 SSO/OIDC
与个人实名归因列为后续项，不作为本轮录制准备的阻塞条件。后端账号验证、短时动作绑定、
人工暂停、MCP 隔离及审计约束仍必须真实生效；不恢复静态审批 key，不让 Agent 自批。
下表中的生产身份能力保持“未实现”，不因范围确认改写为通过。

| 要求 | 当前证据 | 判断 |
|---|---|---|
| 删除共享静态 approver key 路径 | UI 不带 approver key；API 只接受短时动作证明；31 项 HITL/API 测试通过，含静态 key 拒绝 | 完成 |
| sub/auth_time、案件/审批单/决定绑定、新鲜度、防重放 | `hitl.py`、API、存储状态约束；跨案/跨动作/陈旧认证与重复提交测试；已有两案真实认证/审批审计 | 账号级机制完成 |
| 具体自然人归因，而非共享 actor | 当前允许的是共享演示 Matrix admin，记录了账号 subject，但不能证明具体个人所有权 | 后续项，未实现；用户已确认不阻塞本轮演示 |
| 原文的企业 SSO/OIDC 登录接入 | 当前使用 Matrix 密码认证，不存在 OIDC 登录或企业 SSO/MFA 验收 | 后续项，未实现；用户已确认不阻塞本轮演示 |
| Agent 不能自批，到人审停止 | 无审批/恢复 MCP Tool；QA 案 8/8 调查后 WAITING_HUMAN，approval=PENDING，executions=0 | 完成 |
| 带外独立页面 | 真实审批弹窗能打开和切换动作；没有把测试脚本叫作真人点击 | 界面完成；正式本人操作待录 |
| Worker 经 Higress MCP，后端凭证隔离 | 0002/0008 所有业务任务 `skill_transport=higress-mcp`；私密后端 key 在网关，旧 Worker key 无效 | 完成 |
| actor 拆 Server、精确授权、发现与调用边界 | 9 Server/16 Skill；11:20 重验 9 次 tools/list=200、72 次跨角色 401/403，全通过 | 完成 |
| MCP 失败不得冒充成功或回退 REST | Adapter 的 MCP isError/HTTP 失败测试与持久化错误，GJSON 重复编码修复 | 完成 |
| 原文授权命令语义 | 实际版本 PUT 是追加，不是原文所说的 REPLACE；脚本改用限定 Server 的撤权并再授权 | 已按实际接口修正 |
| WebUI 与旁白相符 | 0008 正常/0002 回滚/QA 人审分别标注；5 前端状态测试、截图、任务和 Trace 对应；完整当前数据版脚本 | 完成当前数据分镜对齐 |
| 完成态进度与实际分支一致 | 0008 原先显示 17/20，20 是含回滚分支的计划上限；17 个实际任务均成功。只读投影按实际任务计数，保留 planned_total_tasks，不修改存储状态。12:00 上线返回 17/17，浏览器任务账本和运行进度均已确认；0002 仍为 20/20，QA 仍待审批且无执行 | 已部署；API 回归、浏览器及线上数据无损复核通过 |
| 正式成片可读、本人审批和 Matrix 现场片段 | 1280×720 截图已验；1920 视口 DOM 无横向溢出，但截图工具有裁切/拼接问题；已给出本人操作和原片检查清单 | 交付后的录制操作由组员完成；不宣称成片已验收 |

## 结论与下一步

在上述用户确认范围内，MCP、账号级 HITL、主状态流和当前数据脚本已验证。
0002 不再卡住；无需新增个人账号或部署 SSO 才能录本轮演示。
完成态进度显示修正已于 12:00 部署，0008 的 dashboard 返回按实际任务计数的 17/17；
三条重点案件排除进度展示字段后的完整数据哈希与部署前一致，未清库、重跑或自动审批。
SSH 使用文档中未修改的原凭证即可通过显式密码认证登录，之前的认证失败并不证明密码错误。
随后浏览器已确认 0008 的“已完成”“进度 17 / 17”“正常闭环”“无需回滚”。
本轮软件与稿件对齐目标完成，可按当前数据版录制；剩余人审实录、Matrix 房间素材和
原片清晰度检查是已交付清单中的录制操作，不是未完成的软件修改。
这不代表两份原文中的生产身份要求全部实现，也不代表正式视频已经录制或验收。

录制时本人在审批弹窗登录 `admin`（页面显示“财务负责人（演示）”），再亲自确认批准；
不使用服务器 `root`，不展示密码、不用脚本代录。QA 案保留等待，未因本次范围确认自动批准。
成片用语为“演示审批账号验证＋人工确认”，不使用“企业实名审批”“SSO 已接入”等说法。
SSO/OIDC、个人身份治理与生产安全能力保留在后续接入清单。

详见 [HITL/MCP 实施说明](hitl-mcp-recording.md)、[运行验收](runtime-acceptance-2026-08-31.md)、
[截图审查](ui-recording-audit-2026-08-31.md) 和 [当前数据版旁白](demo-script-current-data.md)。
