# 决赛前整项目复核（2026-09-12 收尾）

目标是检查代码、实际运行、复现入口、前端和比赛材料的一致性；不能仅凭原有绿色测试宣布整项目审查完成。所有构建、测试、浏览器与故障验证在 10.10.10.202 Docker 中执行，保留原 8 个演示案件、执行记录和账务。

## 本轮有证据的发现

| 发现 | 影响 | 处理及验证 |
| --- | --- | --- |
| 只有旧核心表时，应用 Schema 检查仍成功 | 全量部署遗漏资金迁移后，直到网关初始化或资金功能才失败 | 隔离 PG 复现 `RuntimeError not raised`；迁移/应用共用清单，检查全部必要表；缺表拒绝启动，重复迁移保留审计历史 |
| `deploy_demo.sh --full` 只运行 `001_core.sql`，未组合观测覆盖层 | 新部署缺少资金表，更新 API 会丢失 OTel/Grafana 配置 | 改为构建后短时容器调用完整迁移入口；完整部署加入观测和共享看板；无法读取活动运行时拒绝部署 |
| 常用 PG 目标只跑原有 2 项，release 不要求 PG | 资金恢复和真实数据库迁移可被跳过 | `verify-release` 强制 PG 门禁；22 项包含恢复、迁移与既有集成；新增一次性 Docker 验证入口，独立网络且不挂载生产卷 |
| Vite / PostCSS 等存在 5 项 npm 审计问题（4 high、1 moderate） | 构建工具仍携带已知漏洞 | Vite 更新为 6.4.3，修复传递依赖，npm 审计为 0；完整门禁、构建和浏览器验证通过 |
| README、部署/依赖文档与现状不符 | 容易把读取偏差、旧任务数、无 MCP、标准库边界或历史 CI 当作当前事实 | 更新当前文档，保留历史证据；明确资金结果未知恢复、Grafana、依赖和 202 Docker 入口 |

0.5.2 的部署门禁验收见 [project-audit-20260912](evidence/project-audit-20260912/README.md)，0.5.3 的运行互斥与未知资金回执验收见 [runtime-safety-20260912](evidence/runtime-safety-20260912/README.md)。0.5.4 批次已部署至 202，223 项测试与发布门禁通过，覆盖率 90%，发布镜像的 Debian/Python 可修复 HIGH/CRITICAL 扫描为 0，原 8 个案件、执行记录、9 条 ledger 和 261 行审计链前后一致。详见 [recording-consistency-20260912](evidence/recording-consistency-20260912/README.md)。这些结果证明本轮修复，不代表整项目复核已经结束。

HTTP Skill 任务绑定、`RESULT_UNKNOWN` 回执、运行与后台任务互斥、能力续签失败时的恢复状态保留已完成验证。单案重新准备和全量重置的网关/案件/审计已纳入同一数据库事务；报告、Trace 导出与 Case Memory 通过录制批次隔离，原文件保留。隔离数据库拒绝写入、审计失败、旧文件只读和旧 JSON 损坏均纳入本轮故障测试。

0.5.5 已修正人工审批与案件迁移分开提交的问题，以及审批已通过但执行交接中断后无法恢复的边界。审批网关、投影、审计、状态和排队一次提交；驳回同时取消未完成任务。普通状态迁移也以事务提交审计，并用状态代次拒绝旧回调。新增 SQLite/PostgreSQL 故障与并发测试，最终 241 项测试覆盖、91% 显示覆盖率；隔离浏览器走通审批失败、正常审批和启动中断恢复。原生产案件和账务保持不变，详见 [approval-consistency-20260912](evidence/approval-consistency-20260912/README.md)。

0.5.6 已完成案件快照/Matrix 进度的版本检查、StageTask 原子领取与结果审计、旧 attempt 拒绝和安全元数据合并。267 项测试、91% 显示覆盖率与隔离浏览器通过，生产演示数据不变，见 [workflow-concurrency-20260912](evidence/workflow-concurrency-20260912/README.md)。

0.5.7 修正已经领取任务的旧 Worker 在人工恢复后继续入账的问题。工具效果事务检查领取身份与案件版本；人工恢复取消旧任务，并将资金对账、续签、解冻、投影、排队及审计一次提交。旧后台错误不能冻结新恢复代次；回滚凭证在权威资金状态和投影中同步保存。浏览器进一步发现 EXECUTING 恢复跳过权限检查，已补上待写入授权和主库证明的只读核验分支。291 项测试、91% 显示覆盖率和真实数据库拒绝恢复的浏览器场景通过，见 [recovery-consistency-20260912](evidence/recovery-consistency-20260912/README.md)。

0.5.8 已修正页面状态颜色、窄屏数据声明、配置冒充连接状态和迟到案件响应覆盖问题。13 项前端测试、构建、生成契约和浏览器验证通过。独立 Docker daemon 实际跑通最小拓扑的首次部署及重复升级，8 个初始案件和账务保留；生产原案件与审计也保持不变，Grafana 12 个面板通过。根仓库已核对初赛 26 页、复赛 30 页 PPT 及当前问答，修正文件路径和过时口径，第一题及历史 PPT 保留。详见 [ui-cold-review-20260912](evidence/ui-cold-review-20260912/README.md)。

## 收尾验收

- 0.5.9 已完成完整 AgentTeams 前提环境下的冷部署和重复升级，修正 PolarDB 初始化期间的过早就绪，并把独占运行屏障覆盖到构建后停机阶段。维护封锁跨重启保留，失败重跑后才解除；现网原案件和审计保持不变。发布门禁、75 项 PostgreSQL、20 项部署/运行保护、91% 显示覆盖率和浏览器维护提示通过；可修复 HIGH/CRITICAL 为 0。详见 [deployment-safety-20260912](evidence/deployment-safety-20260912/README.md)。
- 0.5.10 在真实 MCP 与人工恢复入口复现旧 Verifier 覆盖新验证记录，已将证据、审批、草稿、验证与案件版本检查和状态迁移纳入同一事务。14 项新增 SQLite/PostgreSQL 检查通过；完整门禁发现 317 项测试，235 项主套件与 82 项 PG 分开执行，覆盖率显示 91%，105/105 确定性评估通过。详见 [coordinator-consistency-20260912](evidence/coordinator-consistency-20260912/README.md)。
- 本轮业务 UI 五种状态、传输失败、迟到请求和四档宽度已验证；资金恢复故障浏览器证据沿用 0.5.7。
- 决赛答辩、历史 PPT/脚本与当前工程证据的差异已逐项记录于根仓库 `submission/决赛材料核对与演示口径.md`；历史录像不能充作当前现场运行。
- 0.5.10 发布沿用独占运行租约与持久维护标记；数据库、私密配置和旧镜像已备份。原 8 个案件、审批/验证/证据/任务、执行记录、9 条 ledger 及 261 行有效审计链前后哈希一致。Gitee 根仓库与 GitHub 子树同步交付。

本次工程复核覆盖业务计算与恢复、审批和 Worker 并发、录制隔离、部署、观测与 demo-ui，以及比赛材料口径；清单内已复现的缺陷均已修复并附隔离验证和发布证据。归档路径另核对了录制批次隔离、活动运行租约与终态恢复限制。企业真实收益、外部 ERP Saga、云 PolarDB HA/PITR 和异地主机监控继续明确标记为未验收，不能用单机合成实验替代；未发现问题也不等于软件不存在任何缺陷。
